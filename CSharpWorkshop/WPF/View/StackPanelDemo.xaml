<Window x:Class="WPF.View.StackPanelDemo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.View"
        mc:Ignorable="d"
        Title="选择题" Height="450" Width="800">
    <Grid>
        <GroupBox Header="请达择没有错别字的成语" BorderThickness="1" BorderBrush="Black" Margin="5">
            <!--Orientation:决定内部元素是横向累积还是纵向累积
            HorizontalAlignment：决定内部元素水平方向上的对齐方式
            VerticalAlignment：决定内部元素竖直方向上的对齐方式-->
            <StackPanel Margin="5">
                <CheckBox Content="A.迫不急待"/>
                <CheckBox Content="B.首曲一指"/>
                <CheckBox Content="C.陈词烂调"/>
                <CheckBox Content="D.哀声叹气"/>
                <CheckBox Content="E.不可礼喻"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="清空" Width="60" Margin="5"/>
                    <Button Content="确定" Width="60" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
